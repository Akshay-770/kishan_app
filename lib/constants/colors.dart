import 'package:flutter/material.dart';

class AppColors {
  // Primary color palette
  static const Color primary = Color(0xFFC26900); // Deep Orange Shade
  static const Color primaryLight = Color(0xFFFFF7A2);
  static const Color primaryDark = Color(0xFF924B00);

  // Background and surface colors
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFF9F9F9);

  // Text colors
  static const Color textPrimary = Color(0xFF3A3A3A);
  static const Color textSecondary = Color(0xFF777777);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Accent colors
  static const Color accent = Color(0xFFFECC6D);
  static const Color accentLight = Color(0xFFFEE8B1);

  // Error color
  static const Color error = Color(0xFFB00020);

  // Other common colors
  static const Color divider = Color(0xFFE0E0E0);
  static const Color shadow = Color(0x1A000000);

// Add any additional custom colors here
}
